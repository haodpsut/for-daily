# Pip fallback for environments without conda (e.g., Colab).
# For GPU simulator on RTX 4090, install pennylane-lightning-gpu separately:
#   pip install pennylane-lightning-gpu --extra-index-url https://pypi.nvidia.com

# --- Core deep learning ---
torch==2.3.*
torchvision==0.18.*
# On Colab with pre-installed CUDA, above usually works. For RTX 4090 local:
#   pip install torch==2.3.* --index-url https://download.pytorch.org/whl/cu121

# --- Graph learning ---
torch-geometric==2.5.*

# --- Quantum ---
pennylane==0.36.*
pennylane-lightning==0.36.*
qiskit==1.1.*
qiskit-aer==0.14.*
qiskit-ibm-runtime==0.24.*

# --- Scientific stack ---
numpy==1.26.*
scipy==1.13.*
scikit-learn==1.5.*
pandas==2.2.*
matplotlib==3.9.*
seaborn==0.13.*

# --- Classical optimization (baseline) ---
cvxpy==1.5.*

# --- Config / logging / tracking ---
wandb==0.17.*
hydra-core==1.3.*
omegaconf==2.3.*
rich>=13
tqdm

# --- Dev tooling ---
pytest>=7.4
black>=24.0
ruff>=0.4
